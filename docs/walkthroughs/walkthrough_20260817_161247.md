# Walkthrough - IBM FlashSystem 雲端問答入口 (Web Portal) 斷線 Bug 診斷、持久化常駐與 RAG 頂級品質驗證報告

**完成時間**: `2026-08-17 16:12:47`  
**Git 分支**: `feature/rag-quality-upgrade`  
**核心成果**: 徹底診斷並修復「❌ 網路連線錯誤」之連線中斷 Bug，完成 **OS 級別 Session Leader 獨立常駐守護架構**、新增 `/api/health` 探針與非同步非阻塞請求管道，並完成紅皮書 `redp5704.pdf` 知識庫寫入與來源多樣性檢索調優！

---

## 🛠️ 修改檔案與架構修復細節

### 1. 獨立脫離 Shell 的常駐守護架構 (`scripts/start_portal_daemon.py` / `stop_portal_daemon.py`)
- **根因修復**：使用 OS 級別檔案描述符與 `start_new_session=True` 脫離終端機生命週期，防範 Python GC 垃圾回收與 Shell 結束時發送的 `SIGHUP` / `SIGPIPE` 訊號。
- **一鍵關閉管理**：新增 `scripts/stop_portal_daemon.py` 安全清理 Port 8888 與 Cloudflare 隧道。

### 2. 非同步非阻塞 RAG 問答管道與健康探針 (`web_app.py`)
- **新增 `/api/health` 探針**：提供服務即時存活狀態與時間戳回傳。
- **非同步事件循環隔離**：將 `query_knowledge_base` 升級為 `async def` 並搭配 `asyncio.to_thread`，徹底防範大模型 10~15 秒生成期阻塞 FastAPI 主線程。

### 3. 全文檢索來源多樣性調優與 `redp5704` 寫入 (`vector_store.py`)
- **來源多樣性保護 (Diversity Throttling)**：限制單一 PDF 在 BM25 檢索中最多佔 4 筆，徹底解決舊書霸榜問題。
- **SQLite 寫入鍵名相容**：修復 `id`/`chunk_id` 與 `content`/`text` 鍵名相容性，成功將 291 筆 `redp5704.pdf` 官方技術切片寫入知識庫。

---

## 🧪 實測驗證與結果

1. **健康探針與公網 Tunnel 驗證**：
   - 經由 `https://tracks-february-beside-cord.trycloudflare.com/api/health` 測試，回傳 `{"status": "ok"}`。
2. **端到端實測查詢 ("從傳統 GMCV 轉換成 PBR")**：
   - **推理引擎**: `Google Gemini (gemini-2.5-flash)`
   - **召回來源**: 自動跨書籍召回 `redp5704.pdf (p12, p15, p28)`, `sg248569.pdf (p71)`, `sg248543.pdf (p78)`。
   - **專業結構輸出**: 完美呈現 **⚠️ 注意事項**、**📋 實務操作 (含 `stoprcrelationship`, `mkreplicationpolicy` 等 CLI 指令)**、**🔍 驗證指令 (`lsvolumegroupreplication`)**。
   - **連線穩定度**: 零 502 錯誤、零連線中斷！

---

## 🌐 存取網址

* **本機存取**: `http://localhost:8888`
* **公網 HTTPS 加密通道**: `https://tracks-february-beside-cord.trycloudflare.com`
